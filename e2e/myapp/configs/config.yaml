app:
  name: "myapp"
  host: "0.0.0.0"
  port: 8080
  env: "development"
# crank:config-section
logging:
  level: "info"
  format: "json"
  # Set to true to include compressed source locations (e.g. handler/user.go:42)
  add_source: false
