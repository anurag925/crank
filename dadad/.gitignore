# Binaries
/bin/
/tmp/

# Local config
.env
*.local.yaml

# IDE
.idea/
.vscode/
*.swp

# Build artifacts
/dist/
/out/

# Generated Swagger spec (re-generate with `crank swag`).
# docs/docs.go is committed so the project always compiles; swag overwrites it.
/docs/swagger.json
/docs/swagger.yaml
