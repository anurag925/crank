package model

// User is the domain model for application users. The auth feature extends the
// base model with a hashed password used by the JWT auth flow.
type User struct {
	ID       int64  `json:"id"`
	Name     string `json:"name"     validate:"required,min=2,max=100"`
	Email    string `json:"email"    validate:"required,email"`
	Password string `json:"-"        validate:"required,min=8"`
}

// APIError is the standard JSON error envelope returned by all endpoints.
// For validation failures, Errors contains per-field messages keyed by
// the JSON field name.
type APIError struct {
	Error   string            `json:"error"             example:"validation failed"`
	Details map[string]string `json:"details,omitempty"`
}
