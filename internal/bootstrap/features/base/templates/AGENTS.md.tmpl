# Agent Guide

This project was generated by Crank.

The project root contains `.crank.yaml`. Use it as the source of truth for enabled Crank features.

Use the system-installed `crank` CLI for common project tasks:

```bash
crank run
crank build
crank test
crank gofmt
crank vet
crank tidy
crank swag
crank migrate up
```

Do not use `./crank`.

If running commands from outside the project root, pass `--project`:

```bash
crank test --project /path/to/project
```

If `.crank.yaml` is missing, do not assume the repository is managed by Crank.

This repo includes a project-local Zed skill at:

```txt
.agents/skills/crank-project/SKILL.md
```

Use that skill for Crank-specific workflow guidance.
