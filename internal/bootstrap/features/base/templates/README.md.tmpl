# {{.ProjectName}}

Production-ready Go backend scaffolded by [rev](#).

## Stack

- HTTP: [Echo v4](https://echo.labstack.com)
- Config: [Viper](https://github.com/spf13/viper) (`configs/config.yaml` + env vars)
- Docs: [Swagger](https://swagger.io) via [swaggo/swag](https://github.com/swaggo/swag) (`/swagger/*`)
- Logging: `log/slog`
{{- if .Has "auth"}}
- Auth: JWT ([golang-jwt/jwt](https://github.com/golang-jwt/jwt))
{{- end}}
{{- if .Has "postgres"}}
- Database: PostgreSQL via [Bun](https://bun.uptrace.dev)
- Migrations: [golang-migrate](https://github.com/golang-migrate/migrate)
{{- end}}

## Quick start

```bash
# 1. fetch dependencies
rev tidy

# 2. copy environment template and adjust as needed
cp .env.example .env

# 3. run the server
rev run
```

The server listens on `http://localhost:8080` by default. Health probe: `GET /health`.
API docs (Swagger UI): `http://localhost:8080/swagger/index.html` (run `rev swag` first).

## Configuration

`configs/config.yaml` ships with safe defaults that are safe to commit. Local secrets belong in
`.env` (git-ignored). Environment variables override file values; keys are derived from
the dotted path with underscores (e.g. `database.password` -> `DATABASE_PASSWORD`).

## Layout

```
.
├── cmd/server          # application entry point
├── configs             # config files (golang-standards/project-layout)
│   └── config.yaml     # non-secret defaults
├── internal
│   ├── config          # Viper-backed configuration
│   ├── handler         # HTTP handlers
│   ├── middleware      # Echo middleware
│   ├── model           # domain models
│   ├── repository      # data access
│   └── service         # business logic
{{- if .Has "postgres"}}
├── migrations          # golang-migrate SQL files
{{- end}}
├── .env.example        # environment template
├── Makefile
├── Dockerfile
└── .air.toml           # live-reload config
```

## Tasks

Common development tasks are provided by the [`rev`](https://github.com/anurag925/rev)
CLI, so they are not duplicated in the `Makefile`. `rev` auto-installs missing
tools (`swag`/`air`/`migrate`) and accepts `--project` to target this directory
from anywhere:

| Command | Purpose |
| ------- | ------- |
| `rev build` | Compile a static binary into `bin/{{.ProjectName}}` |
| `rev run` | Run the application |
| `rev dev` | Run with live reload (air) |
| `rev test` | Run all tests |
| `rev tidy` | Sync module dependencies |
| `rev gofmt` | Run `gofmt` |
| `rev vet` | Run `go vet` |
| `rev swag` | Generate Swagger/OpenAPI docs into `docs/` |
{{- if .Has "postgres"}}
| `rev migrate up` | Apply all pending migrations |
| `rev migrate down --steps 1` | Roll back the last migration |
| `rev make migration <name>` | Generate a new up/down migration pair |
{{- end}}

Run `rev tools` to list everything available.

## Makefile

The `Makefile` intentionally holds only targets that `rev` does not provide
natively (e.g. `make clean`). It's the place for your own project-specific
targets. Any target you add can be run with either `make <target>` or
`rev <target>` — `rev` transparently delegates unknown commands to `make`:

```bash
make clean              # remove build artifacts
rev clean               # same thing, delegated to make
rev <your-make-target>  # any custom target you add to the Makefile
```
