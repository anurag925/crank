package {{.R.Snake}}

import "errors"

// {{.R.Pascal}}ID is a typed identifier for {{.R.SnakePlural}}. Wrapping the
// primitive string in a named type makes it impossible to accidentally pass
// another resource's id where a {{.R.Snake}} id is expected.
type {{.R.Pascal}}ID string

// ErrInvalid{{.R.Pascal}}ID is returned when a {{.R.Snake}} id is empty.
var ErrInvalid{{.R.Pascal}}ID = errors.New("invalid {{.R.Snake}} id")

// New{{.R.Pascal}}ID constructs a {{.R.Pascal}}ID from a raw string, rejecting empty values.
func New{{.R.Pascal}}ID(s string) ({{.R.Pascal}}ID, error) {
	if s == "" {
		return "", ErrInvalid{{.R.Pascal}}ID
	}
	return {{.R.Pascal}}ID(s), nil
}

// String returns the underlying string form of the id.
func (id {{.R.Pascal}}ID) String() string { return string(id) }

// IsZero reports whether the id is the zero value.
func (id {{.R.Pascal}}ID) IsZero() bool { return id == "" }
