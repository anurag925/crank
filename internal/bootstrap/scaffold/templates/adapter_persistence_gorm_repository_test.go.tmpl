package gorm

import (
	"testing"

	"{{.ModulePath}}/internal/domain/{{.R.Snake}}"

	"github.com/stretchr/testify/assert"
)

// With gorm the repository needs a live database, so the CRUD methods
// are not exercised here. These tests cover the parts that do not require a
// *gorm.DB (constructor, error sentinel wiring).
func TestNew{{.R.Pascal}}Repository(t *testing.T) {
	assert.NotNil(t, New{{.R.Pascal}}Repository(nil))
}

func Test{{.R.Pascal}}Repository_UsesAggregateTableName(t *testing.T) {
	// The aggregate's TableName method is used by GORM, so it must
	// return the correct table name.
	assert.Equal(t, "{{.R.SnakePlural}}", (&{{.R.Snake}}.{{.R.Pascal}}{}).TableName())
}
